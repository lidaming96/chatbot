# 智能助手项目分享文档

## 📋 项目概述

本项目是一个基于Streamlit的智能聊天助手，具备**长期记忆能力**。与传统聊天机器人不同，该助手能够记住用户的个人信息、历史对话和上传的文档内容，为用户提供更加个性化和连贯的对话体验。

---

## 🎯 项目背景

### 问题痛点
- **传统聊天机器人的局限性**：每次对话都是独立的，无法记住用户之前的信息
- **个性化服务需求**：用户希望AI助手能了解自己的背景、偏好和历史
- **文档信息利用**：用户需要AI能理解和记住上传的个人文档内容

### 解决方案
- 构建**长期记忆系统**，持久化存储用户信息
- 实现**多维度记忆管理**：事件记忆、用户画像、对话摘要、文档记忆
- 提供**可视化记忆界面**，让用户直观了解AI对自己的认知

---

## 🏗️ 架构设计

### 技术栈（与 `requirements.txt` 对齐）

| 类别 | 技术选型 |
|------|----------|
| 前端 / 应用壳 | Streamlit；根目录 **`Chatbot.py`** 为启动入口，**`core/ui_app.py`** 中 `main()` 负责会话状态驱动的「伪多页」路由 |
| 大模型 API | **DeepSeek**（文本对话、部分 Vision）与 **豆包 Doubao**（火山方舟 OpenAI 兼容接口；图片理解优先） |
| API 封装 | `client.py`：`call_llm_api`、`stream_response`、`analyze_image_with_vision` 统一入口，按 `provider` 切换 |
| 记忆与业务核心 | **`core/`** 包：认证与记忆文件（`auth_memory`）、结构化画像（`structured_profile`）、文档/图片与对话记忆（`documents` / `images` / `conversation_memory`）等；**`Chatbot.py`** 仅聚合导出，兼容原有 `from Chatbot import ...` |
| 工具与提示词 | **`utils/`**：如 `profile_extraction_prompts`、**`web_search.py`**（DuckDuckGo 封装，供多助手复用）等 |
| 本地知识检索（RAG） | LangChain Community：`HuggingFaceEmbeddings` + **FAISS**；PDF 文本抽取链：**PyPDF2 / PyMuPDF / pdfplumber**；扫描件可选 **PaddleOCR** |
| 联网检索 | 依赖 **duckduckgo-search**；业务侧统一通过 **`utils/web_search.search_web`**（投资、美食、旅行等页） |
| Agent | 投资助手：**ReAct** 循环 + 搜索 + 复利计算工具 |
| 数据持久化 | `chat_memories/` 下按用户 JSON；`users.json` 用户表；RAG 向量缓存目录 `rag_cache/` |
| 可选依赖 | `ollama` 等用于实验页（如 `pages/rag_test.py`），主流程以云端 API 为主 |

### 系统架构图
```
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│   用户界面层     │    │   业务逻辑层     │    │   数据存储层     │
├─────────────────┤    ├─────────────────┤    ├─────────────────┤
│ • 登录/注册     │    │ • 用户认证       │    │ • users.json    │
│ • 菜单路由      │────│ • 长期记忆       │────│ • 每用户记忆 JSON│
│ • 各助手对话 UI │    │ • 文档/图片入库  │    │ • RAG 向量缓存   │
│ • 记忆管理页    │    │ • RAG 检索       │    │ • data/*/ PDF 源 │
└─────────────────┘    └─────────────────┘    └─────────────────┘
                              │
                    ┌───────────────────────────┐
                    │   外部服务层               │
                    ├───────────────────────────┤
                    │ • DeepSeek / 豆包 API     │
                    │ • DuckDuckGo（联网搜索）   │
                    └───────────────────────────┘
```

### 项目目录结构（当前）

```
chatbot/
├── Chatbot.py              # Streamlit 启动入口；从 core 再导出符号，保持 pages 中「from Chatbot import …」不变
├── client.py               # DeepSeek / 豆包 客户端与统一调用接口
├── core/                   # 原 Chatbot 主体逻辑（按职责拆分）
│   ├── __init__.py         # 汇总导出，供 Chatbot.py 引用
│   ├── paths.py            # MEMORY_DIR、users 路径等常量
│   ├── auth_memory.py      # 注册登录、记忆 JSON 读写、去重、get_memory_context
│   ├── structured_profile.py  # 画像展示/字段合并、regenerate（LLM 综合）、integrate_all_memories_to_profile
│   ├── documents.py        # PDF/TXT 解析、extract_document_facts、update_document_memory
│   ├── images.py           # 图片 data URL、update_image_memory；解析结果写入 session 供 rerun 后展示
│   ├── conversation_memory.py  # 对话摘要、extract_key_facts、update_memory_system
│   └── ui_app.py           # main()：登录、菜单、各页路由、侧边栏
├── requirements.txt
├── requirements-dev.txt    # 开发依赖（如有）
├── pages/                  # 功能页面（由 ui_app.main 按 current_page 动态加载）
│   ├── memory_management.py
│   ├── work_assistant.py
│   ├── fitness_coach.py
│   ├── family_doctor.py    # + RAG（data/doctor）
│   ├── food_expert.py      # + utils.web_search
│   ├── travel_planner.py   # + utils.web_search
│   ├── investment_assistant.py  # ReAct + utils.web_search + 计算
│   └── …                   # 实验页如 rag_test、search_test 等
├── utils/                  # web_search、profile_extraction_prompts；另有 memory_manager 等与历史脚本共存
├── rag/                    # doctor_rag、food_expert_rag、管线工具
├── chat_memories/          # 用户记忆与 users.json（运行时生成）
├── data/doctor/            # 家庭医生 PDF 知识源（按需放置）
├── data/food/              # 美食 PDF 知识源（rag/food_expert_rag 使用；主界面美食页当前为搜索为主）
├── rag_cache/              # FAISS 等索引缓存
└── .streamlit/secrets.toml # API 密钥（勿提交真实密钥）
```

### 路由方式说明

本项目**未使用** Streamlit 原生的 `pages/` 自动多页导航，而是在 **`core/ui_app.py` 的 `main()`** 中通过 `st.session_state.current_page` 切换模块，并 `import pages.xxx` 调用各页的 `show_*` 函数。根目录 **`Chatbot.py`** 负责 `streamlit run` 时的入口与对外的 API 再导出。启动命令仍为：

```bash
streamlit run Chatbot.py
```

### 核心模块设计

#### 1. 用户认证模块
- **用户注册/登录**：基于用户名密码的认证系统
- **密码安全**：使用 SHA256 哈希存储密码
- **用户隔离**：每个用户拥有独立的记忆空间（独立 JSON 文件）

#### 2. 记忆管理系统
```python
记忆结构 ≈ {
    "summary": "用户总体记忆摘要（可多条）",
    "events": ["具体事件列表"],
    "profile": ["用户画像特征（文本列表）"],
    "structured_profile": {},   # 结构化画像（字典，登录时可与文档/图片合并）
    "facts": [],                # 抽取过程中的事实片段（视实现而定）
    "conversation_history": ["历史对话"],
    "documents": ["文档/图片记忆条目（含 type、filename、摘要或描述等）"],
    "last_updated": "最后更新时间 ISO 字符串"
}
```

#### 3. 对话处理引擎
- **上下文构建**：长期记忆（`get_memory_context`）+ 各页维护的独立 `*_messages` 短期历史
- **流式响应**：`client.stream_response` 对接 DeepSeek / 豆包流式接口
- **记忆更新**：对话后调用 `update_memory_system`，对事件/画像/摘要等去重合并

#### 4. 文档与图片处理管道
```
文档：上传 → PyPDF2/多库解析 → extract_document_facts（LLM）→ 写入 documents + merge + 全量整合 → save
图片：上传 → 多模态 API（优先豆包 Vision）→ 标题/描述/events/profile/structured_profile → 写入记忆
```
- **图片解析结果展示**：入库后会 `st.rerun()` 刷新侧边栏；为避免 rerun 后「已处理文件」分支不再渲染解析 UI，将结果写入 **`st.session_state`**（`core/images.py`：`store_image_memory_parse_result` / `render_last_image_memory_parse`），在 **侧边栏「文档/图片记忆管理」** 与 **记忆管理 → 记忆添加** 中以展开块展示，直至用户点击「知道了」。

#### 5. 结构化画像合并与全量整合（`core/structured_profile.py`）
- **字段合并**：`merge_structured_profile` 对 `health` 等使用 **`merge_health_info`**（新提测覆盖旧健康描述）；对 `basic_info` 等有年龄/历史策略。
- **健康类指标**：若模型把体重/BMI/体脂率写在 `basic_info` 或旧式 `profile` 而 **`health` 为空**，合并前会做 **`_normalize_structured_profile_health`**，从文本中抽取指标填入 `health`，避免合并逻辑跳过健康字段。
- **LLM 再生成**：`regenerate_structured_profile` 会综合多来源文档；生成后对 **`health` 与合并阶段结果一致** 做钉死，减轻「旧文档体脂率覆盖新提测」的问题。
- **全量整合与磁盘一致性**：`integrate_all_memories_to_profile(username, memories=None)` 在 **文档/图片/对话更新流程** 中传入**当前内存中的 `memories`**（含刚 append 的文档、刚 merge 的结构化画像），**避免在未 `save` 前又从磁盘 `load` 旧数据**，导致整合结果错误覆盖刚合并的画像（历史上曾导致侧栏健康/体脂率不更新）。

#### 6. 家庭医生 RAG 子系统（`rag/doctor_rag.py`）
- 从 `data/doctor/` 读取 PDF，多策略抽取文本，必要时 PaddleOCR
- 使用 HuggingFace 嵌入 + FAISS 构建/加载索引（缓存于 `rag_cache/`）
- `family_doctor` 页在对话前检索 Top-K 片段注入系统提示，与长期记忆叠加

---

## 🚀 主要功能

### 0. 功能模块布局

项目采用清晰的模块化设计，将功能分为两大类：

#### ⚙️ 系统功能
- **🧠 记忆管理**：独立的系统功能模块，用于管理用户的长期记忆、查看记忆统计、上传文档/图片、导出记忆数据等

#### 👨‍💼 专业助手
提供多种专业领域的 AI 助手；除下表差异外，均通过 `Chatbot.get_memory_context` 与 `update_memory_system` 接入**同一套长期记忆**。

| 助手 | 记忆 | 增强能力 |
|------|------|----------|
| 工作秘书 / 健身教练 | ✓ | 纯对话 + 流式输出 |
| 家庭医生 | ✓ | **本地 RAG**：`data/doctor/` PDF → FAISS 检索后注入提示词 |
| 美食专家 | ✓ | **`utils/web_search`**（DuckDuckGo），回答可附参考链接 |
| 旅行规划 | ✓ | **`utils/web_search`**（DuckDuckGo） |
| 投资助手 | ✓ | **ReAct** + **`utils/web_search`** + 复利计算工具 |

说明：仓库中另有 `rag/food_expert_rag.py`，可与 `data/food/` 配合做「美食 PDF 知识库」；当前 `pages/food_expert.py` 线上交互以**联网检索**为主，便于获取实时食谱与餐厅类信息。

### 1. 智能对话功能
- **记忆驱动对话**：基于用户历史信息提供个性化回复
- **上下文连贯性**：能够延续之前的话题和讨论
- **实时流式响应**：提供流畅的对话体验

**说明**：`get_memory_context` 实现位于 **`core/auth_memory.py`**：摘要中会**过滤掉以 `【摘要】` 开头的对话摘要行**，仅把偏长期的摘要与文档类内容注入助手上下文（与旧版逻辑一致，避免对话摘要在上下文中重复堆叠）。

### 2. 长期记忆系统

#### 事件记忆
- 自动提取用户提到的具体行动和计划
- 支持模糊去重，避免重复记录
- 可视化显示最近事件

#### 用户画像
- 识别用户的个人属性（职业、兴趣等）
- 动态更新用户特征
- 支持多来源信息融合

#### 对话摘要
- 智能总结对话要点
- 压缩存储长期对话历史
- 保持记忆的时效性

**关键算法：**
```python
def deduplicate_items(existing_items, new_items):
    # 智能去重算法，支持：
    # 1. 完全相同检测
    # 2. 包含关系检测  
    # 3. 被包含关系检测
    unique_new_items = []
    for item in new_items:
        normalized = item.lower().strip()
        is_duplicate = any(
            normalized == exist or
            normalized in exist or  
            exist in normalized
            for exist in normalized_existing
        )
        if not is_duplicate:
            unique_new_items.append(item)
    return existing_items + unique_new_items
```

### 3. 文档与图片记忆功能

#### 支持格式（记忆管理页）
- **PDF / TXT**：多段文本抽取后由 LLM 提取 events、profile、summary、structured_profile
- **图片（PNG/JPG 等）**：走 `client.analyze_image_with_vision`，**优先豆包多模态**，失败时尝试 DeepSeek Vision，输出标题、描述与画像字段并写入 `documents`

#### 处理流程
1. **文件上传**：**记忆管理**页 `pages/memory_management.py`（记忆添加 Tab），或非记忆页 **侧边栏「文档/图片记忆管理」**（`core/ui_app.py`）
2. **内容提取**：PDF/TXT 解析；图片走 Vision API
3. **智能分析**：统一提示词模板（`utils/profile_extraction_prompts.py`）约束 JSON 输出
4. **记忆整合**：去重合并事件/画像/结构化画像；**全量整合**调用 `integrate_all_memories_to_profile(username, memories=当前内存)` 后写回 `chat_memories/<用户>.json`
5. **图片**：上传成功后见侧栏/记忆页 **「图片记忆解析结果（已写入记忆）」** 展开块（见上文核心模块 §4）

**文档处理核心：**
```python
def extract_document_facts(document_text):
    prompt = f"""
    请分析文档内容，提取：
    1. events: 具体事件、行动、经历
    2. profile: 人物属性、特征、技能  
    3. summary: 文档主要内容摘要
    
    文档内容：{document_text[:2000]}
    输出格式：{{"events": [], "profile": [], "summary": ""}}
    """
    # 调用AI分析并解析JSON结果
```

### 4. 记忆可视化界面

#### 实时统计
- 事件数量、画像数量、文档数量
- 记忆更新时间和状态指示

#### 分类展示
- **对话摘要**：橙色标识，显示历史对话要点
- **文档记忆**：蓝色标识，显示文档提取信息
- **来源标识**：区分信息来自对话还是文档

#### 记忆管理
- **导出功能**：支持JSON格式导出所有记忆
- **清除功能**：一键重置所有记忆数据
- **文档管理**：查看已上传文档及提取信息

### 5. 投资助手（ReAct Agent）

投资助手是项目中首个采用**ReAct (Reasoning + Acting)** 思想的智能助手，实现了思考-行动-观察的循环推理模式。

#### ReAct工作原理
1. **思考 (Thought)**：分析用户问题，确定需要执行的操作
2. **行动 (Action)**：执行具体工具操作（搜索、计算等）
3. **观察 (Observation)**：查看行动结果
4. **循环迭代**：根据观察结果继续思考，直到得出最终答案

#### 可用工具
- **🔍 搜索工具 (search)**：使用DuckDuckGo进行实时联网搜索，获取投资信息、股票行情、市场分析等
- **🧮 计算工具 (calculate)**：计算投资回报、复利收益等，支持自定义本金、年化收益率、投资年数等参数

#### 核心实现
```python
def react_agent_iteration(user_query, conversation_history, max_iterations=5):
    """
    ReAct agent主循环：思考-行动-观察
    """
    # 1. 构建系统提示，包含可用工具说明
    # 2. 调用LLM进行思考，解析Thought、Action、Action Input
    # 3. 执行Action，获取Observation
    # 4. 将Observation反馈给LLM，继续思考
    # 5. 重复直到得到Final Answer或达到最大迭代次数
```

#### 功能特点
- **智能推理**：能够根据问题自动选择最合适的工具
- **多轮迭代**：支持多轮思考-行动-观察循环，处理复杂问题
- **实时信息**：通过联网搜索获取最新的市场信息
- **可视化思考过程**：用户可以展开查看AI的完整思考过程
- **记忆集成**：投资助手同样具备长期记忆能力，能够记住用户的投资偏好和历史

### 6. 用户体验优化

#### 安全性
- 密码哈希存储
- 用户数据隔离
- 文件去重处理

#### 性能优化
- 流式响应提升交互体验
- 记忆长度限制防止过载
- 智能上下文裁剪

#### 界面设计
- 响应式布局适配不同屏幕
- 直观的记忆状态指示
- 友好的错误提示和帮助信息

---

## 📈 项目价值

### 技术价值
- **AI记忆系统**：探索了AI长期记忆的实现方案
- **多模态处理**：整合了文本对话和文档处理
- **用户体验**：提供了流畅的交互界面

### 业务价值  
- **个性化服务**：为用户提供定制化的AI助手体验
- **知识管理**：帮助用户更好地组织和利用个人信息
- **效率提升**：减少重复输入，提高沟通效率

### 扩展潜力
- **企业级应用**：可扩展为团队知识管理系统
- **多模态支持**：可增加图片、音频等文件类型
- **云端部署**：支持多用户并发和数据同步

---

## 🔧 快速开始

### 环境要求

- **Python**：建议 3.11（与 `.devcontainer`、`app-testing` CI 一致）
- **安装依赖**（完整功能，含 RAG、OCR、向量库等）：

```bash
pip install -r requirements.txt
```

部分依赖体积较大（如 `paddleocr`、`sentence-transformers`、`faiss-cpu`）。若仅需要「登录 + 对话 + 记忆」而不使用家庭医生 RAG 或扫描版 PDF OCR，可在本地按需裁剪安装，但**推荐以 `requirements.txt` 为准**避免导入错误。

### 配置密钥（`.streamlit/secrets.toml`）

| 变量 | 用途 |
|------|------|
| `DEEPSEEK_API_KEY` | 主对话、文档抽取、部分页面默认 `provider="deepseek"` |
| `DOUBAO_API_KEY` | 豆包文本与 **Vision 图片理解**（记忆管理上传图片时优先） |

示例（请替换为真实密钥，且不要将含密钥的文件提交到 Git）：

```toml
DEEPSEEK_API_KEY = "your_deepseek_key"
DOUBAO_API_KEY = "your_doubao_key"
```

### 家庭医生 RAG 数据

将医学相关 **PDF** 放入 `data/doctor/`。首次进入家庭医生模块时会构建或加载 `rag_cache/` 下的 FAISS 索引；若安装 **PaddleOCR**，可提升扫描版 PDF 的可用性。

### 启动应用

```bash
streamlit run Chatbot.py
```

在 **Dev Container / Codespaces** 中，容器已在 `postAttachCommand` 里配置了类似的 `streamlit run Chatbot.py`（含部分安全相关参数），端口 **8501** 会转发到本机。

### 使用流程
1. **注册 / 登录**：创建账户后，系统会加载或整合该用户的记忆文件
2. **记忆管理**：上传 PDF/TXT/图片、查看统计、导出 JSON、清除记忆；图片上传后可查看 **解析结果展开块**；可选「画像提取测试」等 Tab
3. **选择专业助手**：从首页按钮进入对应模块；家庭医生依赖本地 `data/doctor/` 与索引缓存
4. **投资 / 美食 / 旅行**：依赖 `duckduckgo-search` 时请保证已安装且网络可达
5. **侧边栏**：非记忆页可查看记忆摘要统计，并可在 **文档/图片记忆管理** 中上传文件、查看 **最近一次图片解析**；**登出**会清空会话中的各助手消息状态

---

## 🛠️ 开发与质量保障

- **代码风格**：仓库根目录 `.ruff.toml`；CI 中通过 `streamlit-app-action` 开启 **ruff** 检查
- **自动化测试**：`.github/workflows/app-testing.yml` 在 `main` 分支的 push/PR 上运行 Streamlit 应用检查与 **pytest**（`pytest-args: -v --junit-xml=test-results.xml`）
- **本地开发容器**：`.devcontainer/devcontainer.json` 基于 Python 3.11 镜像，`postCreateCommand` 安装 `requirements.txt`，默认转发 **8501**

---

## 💡 总结

本项目成功实现了一个具备长期记忆能力的智能助手，通过创新的记忆管理系统，为用户提供了更加智能和个性化的对话体验。项目在技术架构、用户体验和功能完整性方面都达到了较高水准，具备良好的实用价值和扩展潜力。

**核心创新点：**
- 🧠 多维度记忆 + **结构化画像**整合（登录与文档/图片入库时合并）
- 📄 文档与 **多模态图片** 入库、智能抽取与去重
- 🎯 **双模型后端**（DeepSeek / 豆包）统一封装，便于扩展 provider
- 📊 记忆管理独立成页，与各助手会话隔离、侧边栏联动统计
- 🏥 家庭医生 **RAG**（PDF → FAISS）与长期记忆联合回答
- 🤖 投资助手 **ReAct** + 搜索 + 金融计算
- 🔍 美食、旅行等场景的 **实时联网检索**
- 🎨 **`core/`** 拆分业务内核 + **`pages/`** 助手页面 + **`utils/web_search`** 公共搜索；根目录 **`Chatbot.py`** 作启动与兼容导出，职责清晰
- 🖼️ 图片入库后 **session 持久展示解析结果**（避免 `rerun` 后界面空白）
- 📌 **全量整合传入内存快照** + **健康字段规范化与钉死**，减少提测数据/体脂率被旧文档或错误 `load` 覆盖